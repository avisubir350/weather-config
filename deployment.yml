apiVersion: apps/v1
kind: Deployment
metadata:
  name: weather-news-deployment
  namespace: weather # Matches the Application destination
  labels:
    app: weather-news
spec:
  replicas: 2
  selector:
    matchLabels:
      app: weather-news
  template:
    metadata:
      labels:
        app: weather-news
    spec:
      containers:
      - name: weather-news-container
        image: avisubir/weather-news-dashboard:7fd2645
        ports:
        - containerPort: 8080
        resources:
          limits:
            memory: "512Mi"
            cpu: "500m"
          requests:
            memory: "256Mi"
            cpu: "250m"
