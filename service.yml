apiVersion: v1
kind: Service
metadata: 
  name: weather-news-service
  namespace: weather
spec:
  selector:
    app: weather-news 
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000 
  type: ClusterIP 